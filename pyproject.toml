[project]
name = "mlops-hw1"
version = "0.1.0"
description = "HW1 - Environment, inference, benchmark"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.2.0",
    "transformers>=4.45.0",
    "accelerate>=0.30.0",
    "pyyaml>=6.0.1",
    "psutil>=5.9.0",
]

[tool.uv]
package = false